# Point the app at a local Thingtime API (api/ runs on PORT, default 3847).
# Use your machine's LAN IP (not localhost) so a physical device can reach it.
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.10:3847
