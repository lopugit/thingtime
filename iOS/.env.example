# Copy to .env for local/TestFlight uploads. Keep .env untracked.
THINGTIME_WEB_URL="https://<vercel-branch-preview-host>"
ASC_KEY_ID="<app-store-connect-api-key-id>"
# Leave blank for an individual App Store Connect API key.
ASC_ISSUER_ID="<app-store-connect-issuer-id-or-blank>"
ASC_KEY_CONTENT="<base64-encoded-p8-content>"
DEVELOPMENT_TEAM="<apple-developer-team-id>"
PRODUCT_BUNDLE_IDENTIFIER=com.thingtime.appletime
WATCH_BUNDLE_IDENTIFIER=com.thingtime.appletime.watchkitapp
# Optional: set when Xcode automatic export cannot create/find the profiles.
PROVISIONING_PROFILE_SPECIFIER="<app-store-provisioning-profile-name>"
WATCH_PROVISIONING_PROFILE_SPECIFIER="<watch-app-store-provisioning-profile-name>"
# Optional: skip Fastlane-managed signing asset sync only when already installed.
SKIP_CERT_SYNC=0
SKIP_PROFILE_SYNC=0
