import { Hamburger } from 'thingtime';

// Rainbow hamburger menu icon: three stacked animated rainbow lines. Any
// Chakra <Flex> style prop passes through to the wrapper.

export const Default = () => (
  <div style={{ padding: '12px 0' }}>
    <Hamburger />
  </div>
);
