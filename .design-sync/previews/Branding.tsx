import { Branding } from 'thingtime';

// Branding is a showcase page: it renders the Logo across its theme + icon
// variants under labelled headings. No props.

export const Showcase = () => <Branding />;
