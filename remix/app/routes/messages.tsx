import { MessengerPage } from '../components/Messenger/MessengerPage';

// /messages — the Thingtime Messenger: Spaces (Slack-style communities with
// channels, sections and threads) and Chats (Messenger-style DMs, groups and
// message requests), one toggle apart.
export default function Messages() {
  return <MessengerPage />;
}
