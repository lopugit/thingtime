import { SchemasPage } from '~/components/Schemas/SchemasPage';

// /docs/schemas — the schema REFERENCE (registry docs). The browsing /
// building experience lives at /schemas (SchemasBrowsePage).
export default function DocsSchemas() {
  return <SchemasPage />;
}
