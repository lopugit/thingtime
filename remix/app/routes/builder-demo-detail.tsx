import { DemoDetailPage } from '~/components/Builder/DemoDetailPage';

// /builder/demos/:slug — one demo, behaviour suite, or app on its own page:
// the catalog preview plus the LIVE version inside the page runtime.
export default function BuilderDemoDetail() {
	return <DemoDetailPage />;
}
