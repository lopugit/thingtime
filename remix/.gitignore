node_modules

.cache
.env
.vercel
.output
dist

build/*
public/build/*
api/index.js
api/index.js.map

# pnpm-lock.yaml

tmp/*
/.swc

# Local media QA process settings
.media-cache-qa.config.cjs
# offline sitemap generator output (npm run sitemap:generate)
sitemap-out/
# filesystem stand-in for the private S3 bucket (THINGTIME_LOCAL_ATTACHMENT_STORAGE_DIR)
.local-attachments/
# seed-fixture state files (ids + generated fixture passwords)
.fixtures/

# Local browser fixtures and isolated QA process configuration
.qa/
