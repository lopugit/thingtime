name: Web CI

# Stable event listener only. The build/test implementation is versioned once
# on github-actions, while checkout inside that implementation still tests the
# exact PR/push commit that triggered this caller.
# Pull requests stay path-filter-free so the protected implementation can
# always report the required full-job or lightweight companion contexts.
on:
  pull_request:
  push:
    branches: [main]
    paths:
      - remix/**
      - .github/workflows/web-ci.yml
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  control-plane:
    uses: lopugit/thingtime/.github/workflows/web-ci.yml@github-actions
    secrets: inherit
